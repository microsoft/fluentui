{
  "name": "nx-workspace-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "nx run nx-workspace-tools:test",
    "lint": "nx run nx-workspace-tools:lint",
    "type-check": "nx run nx-workspace-tools:type-check"
  }
}
