{
  "name": "@fluentui/nx-workspace-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "nx run @fluentui/nx-workspace-tools:test",
    "lint": "nx run @fluentui/nx-workspace-tools:lint",
    "type-check": "nx run @fluentui/nx-workspace-tools:type-check"
  }
}
