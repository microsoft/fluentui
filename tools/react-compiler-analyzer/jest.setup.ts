/* eslint-disable */
// Force plain (non-colored) output so CLI snapshots are deterministic regardless of
// whether the test runner's stdout happens to be an interactive TTY.
process.env.NO_COLOR = '1';
