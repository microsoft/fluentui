import * as React from 'react';

// Component — function returning JSX.Element
export declare function Button(props: ButtonProps): JSX.Element;

// Component — function returning JSX.Element
export declare function Input(props: InputProps): JSX.Element;

// Component — function returning JSX.Element
export declare function FluentProvider(props: FluentProviderProps): JSX.Element;

// Component — function returning JSX.Element
export declare function Tooltip(props: TooltipProps): JSX.Element;

// Hook
export declare function useId(prefix?: string): string;

// Hook
export declare function useToastController(): { dispatchToast: () => void };

// Hook with options
export declare function useArrowNavigationGroup(options?: {
  axis?: 'vertical' | 'horizontal' | 'grid';
  circular?: boolean;
  memorizeCurrent?: boolean;
  tabbable?: boolean;
  unstable_hasDefault?: boolean;
}): Record<string, string>;

// Pure type — interface
export interface ButtonProps {
  appearance?: 'primary' | 'secondary';
  size?: 'small' | 'medium' | 'large';
  icon?: JSX.Element;
  disabled?: boolean;
  children?: React.ReactNode;
}

// Pure type — interface
export interface InputProps {
  placeholder?: string;
  appearance?: 'outline' | 'filled';
  contentBefore?: JSX.Element;
}

// Pure type — interface
export interface FluentProviderProps {
  theme?: Record<string, string>;
  children?: React.ReactNode;
}

// Pure type — interface
export interface TooltipProps {
  content?: string;
  relationship?: string;
  children?: React.ReactNode;
}

// Other — constant (not a component, not a type)
export declare const tokens: Record<string, string>;

// Other — constant (theme object)
export declare const webLightTheme: Record<string, string>;

// Other — constant (PascalCase but NOT a component)
export declare const AzureLightTheme: Record<string, string>;

// Other — utility function
export declare function makeStyles<T extends Record<string, Record<string, unknown>>>(
  styles: T,
): () => Record<keyof T, string>;

// Generic type — for testing generic type reference tracking
export type ColumnDef<TData> = {
  id: string;
  header: string;
  cell: (data: TData) => React.ReactNode;
};

// Other — constant (PascalCase, looks like component but is a string constant)
export declare const Tab: string;

// Other — constant
export declare const Enter: string;
