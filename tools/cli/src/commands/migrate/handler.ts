import type { CommandHandler } from '../../utils/types';

export const handler: CommandHandler = async argv => {
  // TODO: Implement migrate command
  console.log('migrate command - not yet implemented');
};
