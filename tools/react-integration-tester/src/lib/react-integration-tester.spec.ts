import { reactIntegrationTester } from './react-integration-tester';

describe('reactIntegrationTester', () => {
  it('should work', () => {
    expect(reactIntegrationTester()).toEqual('react-integration-tester');
  });
});
