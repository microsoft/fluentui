{
  "$schema": "https://json-schema.org/schema",
  "cli": "nx",
  "$id": "visual-regression-schema",
  "description": "Create visual-regression package within FluentUI v9 component package",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to add visual regression testing to",
      "alias": "p",
      "x-dropdown": "projects",
      "x-prompt": "For which project do you want to add visual regression testing?",
      "x-priority": "important"
    },
    "owner": {
      "type": "string",
      "description": "Team owning the library. Will be written in CODEOWNERS",
      "enum": [
        "@microsoft/cxe-red",
        "@microsoft/cxe-prg",
        "@microsoft/fluentui-react-build",
        "@microsoft/teams-prg",
        "@microsoft/xc-uxe"
      ],
      "x-priority": "important",
      "x-prompt": {
        "message": "Which team will own this library"
      }
    }
  },
  "required": ["project"]
}
