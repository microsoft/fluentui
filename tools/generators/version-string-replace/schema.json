{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "version-string-replace",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Library name",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "bumpType": {
      "type": "string",
      "description": "The type of bump to the version number"
    },
    "prereleaseTag": {
      "type": "string",
      "description": "If bumping prerelease version, the identifier to use"
    },
    "all": {
      "type": "boolean",
      "description": "Run generator on all vNext packages`"
    },
    "replace": {
      "type": "string",
      "description": "Replacement string for the matched version part"
    }
  },
  "required": ["match", "replace", "bumpType"]
}
