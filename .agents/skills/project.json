{
  "name": "agent-skills",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node --test",
        "cwd": ".agents/skills",
        "forwardAllArgs": false
      }
    }
  }
}
