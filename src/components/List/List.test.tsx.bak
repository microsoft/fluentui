import List from './List';

let _instance = 0;

describe('List', () => {

  it('can render one item', () => {
    let list = new List({
      items: [{

      }]
    });
  });

  it('can render more than a page of items', () => {

  });

  it ('can render an offscreen page that contains a focused item', () => {

  });

  it('can render an empty item set', () => {

  });

});


function _getTestItems(count: number) {
  let items = [];

  return items;
}
function _getTestItem(name: string) {
  return {
    key: _instance++,
    name: name
  };
}
