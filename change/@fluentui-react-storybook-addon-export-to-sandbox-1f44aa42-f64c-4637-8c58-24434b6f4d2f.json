{
  "type": "patch",
  "comment": "Fix preset detection on Windows: the `presetsList` path pattern used a POSIX-only `/` separator, so the native backslash paths that `loadWorkspaceAddon` passes in development never matched and `getAddonOptions` silently fell back to defaults, discarding `importMappings`, `webpackRule` and `cssModules`.",
  "packageName": "@fluentui/react-storybook-addon-export-to-sandbox",
  "email": "array.knight+github@gmail.com",
  "dependentChangeType": "patch"
}
