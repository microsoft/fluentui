{
  "type": "patch",
  "comment": "feat: detect non-reactive store snapshot reads (getState()/getXStore().field) on CompileSuccess functions via a new opt-in risk-detection Babel plugin; surfaced as a 'Compiled but Risky' report section configured via --risk-config (validated against a shipped risk-config.schema.json)",
  "packageName": "@fluentui/react-compiler-analyzer",
  "email": "martinhochel@microsoft.com",
  "dependentChangeType": "patch"
}
