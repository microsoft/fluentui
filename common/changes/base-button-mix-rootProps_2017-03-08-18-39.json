{
  "changes": [
    {
      "packageName": "office-ui-fabric-react",
      "comment": "BaseButton: `rootProps` is now correctly mixed in with `props`. Please note that `rootProps` is deprecated and should not be used, but this fix simply ensures that the deprecated backwards compatibility works.",
      "type": "patch"
    }
  ],
  "email": "micahgodbolt@gmail.com"
}
