import * as React from 'react';
import { Link, LinkProps } from '..'; // codesandbox-dependency: @fluentui/react-link ^9.0.0-beta

export const Default = (props: LinkProps) => <Link {...props}>This is a link</Link>;
