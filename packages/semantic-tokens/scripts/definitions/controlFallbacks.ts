import { compoundButtonFallbacks } from './fallbacks/button';
import { GroupFallbacks } from './fallbacks/fallbacks.types';

export const controlFallbacks: GroupFallbacks = {
  compoundbutton: compoundButtonFallbacks,
};
