/* Re-export things from glamor, so that downstream dependencies can use it. */
// export * from 'glamor';
export {
  before,
  after,
  parent,
  select,
  insertGlobal,
  fontFace,
  keyframes
} from 'glamor';
