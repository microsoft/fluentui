module.exports = {
  printWidth: 140,
  tabWidth: 2,
  singleQuote: true,
  // Set these for compatibility with prettier 2 in case it's used by format on save
  trailingComma: 'none',
  arrowParens: 'avoid'
};
