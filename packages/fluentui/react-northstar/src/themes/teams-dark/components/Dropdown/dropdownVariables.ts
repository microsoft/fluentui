import { DropdownVariables } from '../../../teams/components/Dropdown/dropdownVariables';

export default (): Partial<DropdownVariables> => ({
  loadingMessageBackgroundColor: 'transparent',
  noResultsMessageBackgroundColor: 'transparent',
});
