import { isConformant } from 'test/specs/commonTests';
import InputIcon from 'src/components/Input/InputIcon';

describe('InputIcon', () => {
  isConformant(InputIcon, { constructorName: 'InputIcon' });
});
