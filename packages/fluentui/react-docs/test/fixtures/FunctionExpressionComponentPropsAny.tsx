import * as React from 'react';

const FunctionExpressionComponentInterfaceProps = (props: any) => {
  return <div>{props.children}</div>;
};

export default FunctionExpressionComponentInterfaceProps;
