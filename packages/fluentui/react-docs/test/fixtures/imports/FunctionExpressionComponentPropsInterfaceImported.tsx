import * as React from 'react';

import { IPropsChildren } from '../external-component-types';

const FunctionExpressionComponentPropsInterfaceImported = (props: IPropsChildren) => {
  return <div>{props.children}</div>;
};

export default FunctionExpressionComponentPropsInterfaceImported;
