import * as React from 'react';
import { Dropdown } from '@fluentui/react-northstar';

const DropdownExampleHeaderMessage = () => (
  <Dropdown items={[]} headerMessage="We didn't find any items!" placeholder="Empty Dropdown for header message" />
);

export default DropdownExampleHeaderMessage;
