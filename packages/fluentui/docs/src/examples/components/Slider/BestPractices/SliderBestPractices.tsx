import * as React from 'react';

import ComponentBestPractices from '../../../../components/ComponentBestPractices';

const SliderBestPractices = () => (
  <ComponentBestPractices doList={['Provide label by using `aria-label`, or `aria-labelledby` prop.']} />
);

export default SliderBestPractices;
