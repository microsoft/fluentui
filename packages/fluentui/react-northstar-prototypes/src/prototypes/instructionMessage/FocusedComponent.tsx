// import * as React from 'react';
// import { Alert, AlertProps } from '@fluentui/react-northstar';
// // import { useFocusEffect, useIsFocused } from 'react-navigation-hooks';

// interface AnimatedBannerAlertProps extends AlertProps {
//   open?: boolean;
// }

// /**
//  * Needs to have 'slideDown' animation defined in parent Provider
//  */
// const FocusedComponent: React.FunctionComponent<AnimatedBannerAlertProps> = props => {
//   const { open, ...rest } = props;

//   const isFocused = useIsFocused();

//   if (isFocused) return <Alert content="Focused" />;
// };

// export default FocusedComponent;
