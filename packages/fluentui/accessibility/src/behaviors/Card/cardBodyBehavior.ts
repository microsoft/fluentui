import { Accessibility } from '../../types';
/**
 * @description
 * Behavior for a card body component
 * @specification
 * Does nothing
 */
const cardBodyBehavior: Accessibility<CardBodyBehaviorProps> = () => ({
  attributes: {}
});

export default cardBodyBehavior;

export type CardBodyBehaviorProps = never;
