import { Accessibility } from '../../types';
/**
 * @description
 * Behavior for a card preview component
 * @specification
 * Does nothing
 */
const cardPreviewBehavior: Accessibility<CardPreviewBehaviorProps> = () => ({
  attributes: {}
});

export default cardPreviewBehavior;

export type CardPreviewBehaviorProps = never;
