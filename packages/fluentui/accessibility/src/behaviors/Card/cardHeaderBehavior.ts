import { Accessibility } from '../../types';
/**
 * @description
 * Behavior for a card header component
 * @specification
 * Does nothing
 */
const cardHeaderBehavior: Accessibility<CardHeaderBehaviorProps> = () => ({
  attributes: {}
});

export default cardHeaderBehavior;

export type CardHeaderBehaviorProps = never;
