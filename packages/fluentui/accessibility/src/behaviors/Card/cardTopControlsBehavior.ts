import { Accessibility } from '../../types';
/**
 * @description
 * Behavior for a card top controls component
 * @specification
 * Does nothing
 */
const cardTopControlsBehavior: Accessibility<CardTopControlsBehaviorProps> = () => ({
  attributes: {}
});

export default cardTopControlsBehavior;

export type CardTopControlsBehaviorProps = never;
