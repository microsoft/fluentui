import { Accessibility } from '../../types';
/**
 * @description
 * Behavior for a card footer component
 * @specification
 * Does nothing
 */
const cardFooterBehavior: Accessibility<CardFooterBehaviorProps> = () => ({
  attributes: {}
});

export default cardFooterBehavior;

export type CardFooterBehaviorProps = never;
