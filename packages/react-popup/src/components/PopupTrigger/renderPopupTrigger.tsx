import { PopupTriggerState } from './PopupTrigger.types';

/**
 * Render the final JSX of PopupTrigger
 */
export const renderPopupTrigger = (state: PopupTriggerState) => {
  return state.children;
};
