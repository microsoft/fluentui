import { makeStyles, tokens } from '@fluentui/react-components';

export const useStyles = makeStyles({
  root: {},
  insideDonutString: {
    fontSize: tokens.fontSizeBase600,
    fontWeight: tokens.fontWeightSemibold,
    fill: tokens.colorNeutralForeground1Selected,
  },
});
