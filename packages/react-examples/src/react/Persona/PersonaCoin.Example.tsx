import * as React from 'react';
import { PersonaCoin } from '@fluentui/react/lib/Persona';

export const PerconaCoinExample: React.FunctionComponent = () => {
  return <PersonaCoin text="" coinSize={32} />;
};
