import * as React from 'react';
import { Button } from './Button';

export default {
  title: 'Fluent UI Core/Button',
  component: Button,
};

export const Vanilla = (p) => <Button {...p}>Vanilla</Button>;
