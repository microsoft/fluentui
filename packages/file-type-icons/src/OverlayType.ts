/**
 * Enumerates the different overlays you can place
 * over a file type icon.
 */

export const enum OverlayType {
    none = 0,
    block = 1,
    checkout = 2,
    checkoutGrid = 3,
    notify = 4
}