{
  "name": "react-charting",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "tags": ["v8", "ships-bundle", "charting"],
  "implicitDependencies": [],
  "targets": {
    "test": {
      "dependsOn": ["^build", { "projects": ["jest-serializer-merge-styles"], "target": "build" }]
    },
    "type-check": { "command": "echo 'type-check is noop for v8 project which uses tsc for build'" }
  }
}
