import { Separator } from './Separator';
import { isConformant } from '../../common/isConformant';

describe('Separator', () => {
  isConformant({
    Component: Separator,
    displayName: 'Separator',
  });
});
