import { Overlay } from './Overlay';
import { isConformant } from '../../common/isConformant';

describe('Overlay', () => {
  isConformant({
    Component: Overlay,
    displayName: 'Overlay',
  });
});
