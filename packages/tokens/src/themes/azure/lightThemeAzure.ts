import { createAzureLightTheme } from '../../utils/createAzureLightTheme';
import { brandAzure } from '../../global/brandColors';
import type { Theme } from '../../types';

export const azureLightTheme: Theme = createAzureLightTheme(brandAzure);
