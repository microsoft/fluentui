import { createTeamsDarkGlassTheme } from '../../utils/createTeamsDarkGlassTheme';
import { brandTeams } from '../../global/brandColors';
import type { Theme } from '../../types';

export const teamsDarkGlassTheme: Theme = createTeamsDarkGlassTheme(brandTeams);
