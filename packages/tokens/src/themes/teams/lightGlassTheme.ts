import { createLightGlassTheme } from '../../utils/createLightGlassTheme';
import { brandTeams } from '../../global/brandColors';
import type { Theme } from '../../types';

export const teamsLightGlassTheme: Theme = createLightGlassTheme(brandTeams);
