import { ISetupCardsStyles } from './SetupCards.types';

export const getStyles = (): ISetupCardsStyles => {
  return {
    root: {
      overflow: 'visible',
      position: 'relative',
      margin: 'auto',
      height: '276px'
    }
  };
};
