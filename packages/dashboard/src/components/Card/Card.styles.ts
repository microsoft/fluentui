import { ICardStyles } from './Card.types';

export const getStyles = (): ICardStyles => {
  return {
    root: {
      backgroundColor: '#ffffff',
      height: '100%',
      width: '100%'
    }
  };
};
