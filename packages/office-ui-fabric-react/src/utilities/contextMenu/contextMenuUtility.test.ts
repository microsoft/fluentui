import { getIsChecked, hasSubmenu } from './contextMenuUtility';

describe('getIsChecked', () => {
  it('passes', () => {
    expect(true).toBe(true);
  });
});

describe('hasSubmenu', () => {
  it('passes', () => {
    expect(true).toBe(true);
  });
});
