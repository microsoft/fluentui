import { Button } from '../button/button.js';

/**
 * A MenuButton component that provides a customizable button element for triggering menus.
 * @class MenuButton
 * @extends Button
 *
 * @csspart control - The root element of the button.
 * @csspart content - The content of the button.
 *
 * @slot - Default slot for the button's content.
 * @slot start - Slot for content at the start of the button.
 * @slot end - Slot for content at the end of the button.
 *
 * @summary The MenuButton component functions as a customizable button element for triggering menus.
 *
 * @tag fluent-menu-button
 *
 * @public
 */

export class MenuButton extends Button {}
