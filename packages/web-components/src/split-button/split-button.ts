import { FASTElement } from '@microsoft/fast-element';

/**
 * A Split Button Custom HTML Element.
 *
 * @slot - The default slot for Split Button content
 *
 * @public
 */
export class SplitButton extends FASTElement {}
