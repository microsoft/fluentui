import './index';
import '../button';
import '../select';
import '../badge';
import ToolbarTemplate from './fixtures/toolbar.html';

export default {
  title: 'Toolbar',
};

export const Toolbar = () => ToolbarTemplate;
