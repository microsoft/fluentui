import { FASTElement } from '@microsoft/fast-element';

/**
 * A TabPanel component that provides a container for tab content.
 * @class TabPanel
 * @extends FASTElement
 *
 * @slot - The default slot for the tab panel content.
 *
 * @summary The TabPanel component provides a container for tab content.
 *
 * @tag fluent-tab-panel
 *
 * @public
 */
export class TabPanel extends FASTElement {}
