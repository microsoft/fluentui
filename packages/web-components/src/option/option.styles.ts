import { css } from '@microsoft/fast-element';
import { display } from '../utils/display.js';

/**
 * Styles of the {@link (Option:class)} component.
 *
 * @public
 */
export const styles = css`
  ${display('inline-flex')}
`;
