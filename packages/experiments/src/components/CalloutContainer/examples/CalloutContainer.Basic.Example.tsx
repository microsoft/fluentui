import * as React from 'react';
import { CalloutContainer } from '../CalloutContainer';

export class CalloutContainerBasicExample extends React.Component<{}, {}> {
  public render(): JSX.Element {
    return (
      <CalloutContainer />
    );
  }
}