import { TeamsCustomizations } from '@uifabric/theme-samples';
import { Theme } from '@fluentui/react-theme-provider';

export const TeamsTheme: Theme = {
  tokens: TeamsCustomizations.settings.theme,
  stylesheets: [],
};
