import * as React from 'react';
import { SwatchColorPikerCell, SwatchColorPikerCellProps } from '@fluentui/react-swatch-color-picker';

export const Default = (props: Partial<SwatchColorPikerCellProps>) => <SwatchColorPikerCell {...props} />;
