import * as React from 'react';
import { CalendarYear, CalendarYearProps } from '@fluentui/react-datepicker-compat';

export const Default = (props: Partial<CalendarYearProps>) => <CalendarYear {...props} />;
