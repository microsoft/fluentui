import * as React from 'react';
import type { CalendarProps } from './Calendar.types';

/**
 * Calendar component - TODO: add more docs
 */
export const Calendar: React.FC<CalendarProps> = (props, ref) => <></>;

Calendar.displayName = 'Calendar';
