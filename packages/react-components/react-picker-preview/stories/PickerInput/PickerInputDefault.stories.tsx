import * as React from 'react';
import { PickerInput, PickerInputProps } from '@fluentui/react-picker-preview';

export const Default = (props: Partial<PickerInputProps>) => <PickerInput {...props} />;
