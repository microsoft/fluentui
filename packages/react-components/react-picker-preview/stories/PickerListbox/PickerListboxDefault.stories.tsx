import * as React from 'react';
import { PickerListbox, PickerListboxProps } from '@fluentui/react-picker-preview';

export const Default = (props: Partial<PickerListboxProps>) => <PickerListbox {...props} />;
