import * as React from 'react';
import { PickerTagGroup, PickerTagGroupProps } from '@fluentui/react-picker-preview';

export const Default = (props: Partial<PickerTagGroupProps>) => <PickerTagGroup {...props} />;
