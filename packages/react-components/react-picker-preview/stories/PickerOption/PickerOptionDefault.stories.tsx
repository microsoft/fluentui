import * as React from 'react';
import { PickerOption, PickerOptionProps } from '@fluentui/react-picker-preview';

export const Default = (props: Partial<PickerOptionProps>) => <PickerOption {...props} />;
