import * as React from 'react';
import { PickerButton, PickerButtonProps } from '@fluentui/react-picker-preview';

export const Default = (props: Partial<PickerButtonProps>) => <PickerButton {...props} />;
