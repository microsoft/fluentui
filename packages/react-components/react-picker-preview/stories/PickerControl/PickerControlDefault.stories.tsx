import * as React from 'react';
import { PickerControl, PickerControlProps } from '@fluentui/react-picker-preview';

export const Default = (props: Partial<PickerControlProps>) => <PickerControl {...props} />;
