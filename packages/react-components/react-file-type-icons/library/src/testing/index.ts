/**
 * Testing utilities for @fluentui/react-file-type-icons
 *
 * These exports are intended for testing purposes only and should not be used in production code.
 * They provide access to internal state management functions that allow tests to reset and verify
 * the configured base URL state.
 */

export { getConfiguredBaseUrl, resetConfiguredBaseUrl } from '../utils/initializeFileTypeIcons';
