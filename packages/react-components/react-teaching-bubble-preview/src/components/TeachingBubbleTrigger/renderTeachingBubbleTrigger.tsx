import { TeachingBubbleTriggerState } from './TeachingBubbleTrigger.types';

/**
 * Render the final JSX of TeachingBubbleTrigger
 */
export const renderTeachingBubbleTrigger_unstable = (state: TeachingBubbleTriggerState) => {
  return state.children;
};
