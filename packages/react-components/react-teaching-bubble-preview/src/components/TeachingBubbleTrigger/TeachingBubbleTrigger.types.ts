import { PopoverTriggerChildProps, PopoverTriggerProps, PopoverTriggerState } from '@fluentui/react-popover';

/**
 * TeachingBubbleTrigger Props
 */
export type TeachingBubbleTriggerProps = PopoverTriggerProps & {};

/**
 * TeachingBubbleTrigger State
 */
export type TeachingBubbleTriggerState = PopoverTriggerState & {};

/**
 * Props that are passed to the child of the DialogTrigger when cloned to ensure correct behavior for the Dialog
 */
export type TeachingBubbleTriggerChildProps = PopoverTriggerChildProps & {};
