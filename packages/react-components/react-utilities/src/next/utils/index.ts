export { clamp } from '../../utils/clamp';
export {
  // eslint-disable-next-line @typescript-eslint/no-deprecated
  getNativeElementProps,
  getPartitionedNativeProps,
} from '../../utils/getNativeElementProps';
export { getRTLSafeKey } from '../../utils/getRTLSafeKey';
export { mergeCallbacks } from '../../utils/mergeCallbacks';
export { omit } from '../../utils/omit';
export {
  anchorProperties,
  audioProperties,
  baseElementEvents,
  baseElementProperties,
  buttonProperties,
  colGroupProperties,
  colProperties,
  dialogProperties,
  divProperties,
  fieldsetProperties,
  formProperties,
  getNativeProps,
  htmlElementProperties,
  iframeProperties,
  imgProperties,
  inputProperties,
  labelProperties,
  liProperties,
  microdataProperties,
  olProperties,
  optionProperties,
  selectProperties,
  tableProperties,
  tdProperties,
  textAreaProperties,
  thProperties,
  timeProperties,
  trProperties,
  videoProperties,
} from '../../utils/properties';
export { isHTMLElement } from '../../utils/isHTMLElement';
export { isInteractiveHTMLElement } from '../../utils/isInteractiveHTMLElement';
export type { PriorityQueue, PriorityQueueCompareFn } from '../../utils/priorityQueue';
export { createPriorityQueue } from '../../utils/priorityQueue';
