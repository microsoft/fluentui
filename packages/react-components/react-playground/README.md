# @fluentui/react-playground

Prebuilt editor shell used by `@fluentui/react-storybook-addon-playground`.

The package owns Monaco, formatting, URL state and the sandbox host. Consumer design-system packages are compiled into a
separate runtime by the Storybook addon and are not bundled into this package.
