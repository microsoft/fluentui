 
import * as React from 'react';

export interface ProviderContextValueV2 {
  bar: string;
  baz: string;
}

export const ProviderContext = React.createContext<ProviderContextValueV2>({ bar: 'bar', baz: 'baz' });
