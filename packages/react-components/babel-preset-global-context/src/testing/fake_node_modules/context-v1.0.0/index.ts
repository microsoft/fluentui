 
import * as React from 'react';

export interface ProviderContextValue {
  foo: string;
  bar: string;
}

export const ProviderContext = React.createContext<ProviderContextValue>({ foo: 'foo', bar: 'bar' });
