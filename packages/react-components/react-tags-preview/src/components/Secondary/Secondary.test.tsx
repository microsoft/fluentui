import { isConformant } from '../../testing/isConformant';
import { Secondary } from './Secondary';

describe('Secondary', () => {
  isConformant({
    Component: Secondary,
    displayName: 'Secondary',
  });
});
