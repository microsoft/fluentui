import styles from './AttachmentBody.module.css';

const attachmentBodyStyles = {
  root: styles.root,
};

export const useAttachmentBodyStyles = (): typeof attachmentBodyStyles => attachmentBodyStyles;
