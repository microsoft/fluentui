import styles from './AttachmentIcon.module.css';

const attachmentIconStyles = {
  root: styles.root,
};

export const useAttachmentIconStyles = (): typeof attachmentIconStyles => attachmentIconStyles;
