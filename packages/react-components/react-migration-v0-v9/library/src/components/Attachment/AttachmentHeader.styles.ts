import styles from './AttachmentHeader.module.css';

const attachmentHeaderStyles = {
  root: styles.root,
};

export const useAttachmentHeaderStyles = (): typeof attachmentHeaderStyles => attachmentHeaderStyles;
