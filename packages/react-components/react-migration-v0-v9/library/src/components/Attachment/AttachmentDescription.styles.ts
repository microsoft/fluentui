import styles from './AttachmentDescription.module.css';

const attachmentDescriptionStyles = {
  root: styles.root,
};

export const useAttachmentDescriptionStyles = (): typeof attachmentDescriptionStyles => attachmentDescriptionStyles;
