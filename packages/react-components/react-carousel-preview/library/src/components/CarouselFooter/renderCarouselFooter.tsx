/** @jsxRuntime automatic */
/** @jsxImportSource @fluentui/react-jsx-runtime */

import { assertSlots } from '@fluentui/react-utilities';
import type { CarouselFooterState, CarouselFooterSlots } from './CarouselFooter.types';

/**
 * Render the final JSX of CarouselFooter
 */
export const renderCarouselFooter_unstable = (state: CarouselFooterState) => {
  assertSlots<CarouselFooterSlots>(state);

  // TODO Add additional slots in the appropriate place
  return <state.root />;
};
