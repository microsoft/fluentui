import * as React from 'react';
import { CarouselCard, CarouselCardProps } from '@fluentui/react-carousel-preview';

export const Default = (props: Partial<CarouselCardProps>) => <CarouselCard {...props} />;
