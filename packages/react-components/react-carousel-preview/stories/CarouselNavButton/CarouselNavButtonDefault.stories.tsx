import * as React from 'react';
import { CarouselNavButton, CarouselNavButtonProps } from '@fluentui/react-carousel-preview';

export const Default = (props: Partial<CarouselNavButtonProps>) => <CarouselNavButton {...props} />;
