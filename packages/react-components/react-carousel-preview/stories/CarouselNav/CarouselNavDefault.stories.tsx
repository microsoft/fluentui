import * as React from 'react';
import { CarouselNav, CarouselNavProps } from '@fluentui/react-carousel-preview';

export const Default = (props: Partial<CarouselNavProps>) => <CarouselNav {...props} />;
