import * as React from 'react';
import { CarouselNavImageButton, CarouselNavImageButtonProps } from '@fluentui/react-carousel-preview';

export const Default = (props: Partial<CarouselNavImageButtonProps>) => <CarouselNavImageButton {...props} />;
