import * as React from 'react';
import { TableSwatch, TableSwatchProps } from '@fluentui/react-swatch-picker-preview';

export const Default = (props: Partial<TableSwatchProps>) => <TableSwatch {...props} />;
