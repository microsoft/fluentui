import * as React from 'react';
import { RadioSwatch, RadioSwatchProps } from '@fluentui/react-swatch-picker-preview';

export const Default = (props: Partial<RadioSwatchProps>) => <RadioSwatch {...props} />;
