import * as React from 'react';
import { TablePicker, TablePickerProps } from '@fluentui/react-swatch-picker-preview';

export const Default = (props: Partial<TablePickerProps>) => <TablePicker {...props} />;
