import * as React from 'react';
import { SwatchPickerRow, SwatchPickerRowProps } from '@fluentui/react-swatch-picker-preview';

export const Default = (props: Partial<SwatchPickerRowProps>) => <SwatchPickerRow {...props} />;
