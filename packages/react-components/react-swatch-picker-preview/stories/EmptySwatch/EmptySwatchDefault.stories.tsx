import * as React from 'react';
import { EmptySwatch, EmptySwatchProps } from '@fluentui/react-swatch-picker-preview';

export const Default = (props: Partial<EmptySwatchProps>) => <EmptySwatch {...props} />;
