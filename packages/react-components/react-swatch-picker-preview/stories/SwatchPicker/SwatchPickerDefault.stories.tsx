import * as React from 'react';
import { SwatchPicker, SwatchPickerProps } from '@fluentui/react-swatch-picker-preview';

export const Default = (props: Partial<SwatchPickerProps>) => <SwatchPicker {...props} />;
