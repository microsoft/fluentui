import * as React from 'react';
import { ColorSwatch, ColorSwatchProps } from '@fluentui/react-swatch-picker-preview';

export const Default = (props: Partial<ColorSwatchProps>) => <ColorSwatch {...props} />;
