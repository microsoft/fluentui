import * as React from 'react';
import { NavSubItemGroup } from '@fluentui/react-nav-preview';
import type { NavSubItemGroupProps } from '@fluentui/react-nav-preview';

export const Default = (props: Partial<NavSubItemGroupProps>) => <NavSubItemGroup {...props} />;
