import * as React from 'react';
import { AppItemStatic, AppItemStaticProps } from '@fluentui/react-nav-preview';

export const Default = (props: Partial<AppItemStaticProps>) => <AppItemStatic {...props} />;
