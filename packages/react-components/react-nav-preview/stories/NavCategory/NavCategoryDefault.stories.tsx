import * as React from 'react';
import { NavCategory } from '@fluentui/react-nav-preview';
import type { NavCategoryProps } from '@fluentui/react-nav-preview';

export const Default = (props: Partial<NavCategoryProps>) => <NavCategory {...props} />;
