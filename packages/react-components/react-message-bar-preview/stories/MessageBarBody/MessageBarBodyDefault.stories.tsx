import * as React from 'react';
import { MessageBarBody, MessageBarBodyProps } from '@fluentui/react-message-bar-preview';

export const Default = (props: Partial<MessageBarBodyProps>) => <MessageBarBody {...props} />;
