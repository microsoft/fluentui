import * as React from 'react';
import { Card } from '@fluentui/react-components';

export const DefinitionDefault = () => <Card>Default Card</Card>;
DefinitionDefault.parameters = { docs: { description: { story: 'A card is a container with elevation.' } } };
