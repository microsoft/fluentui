import * as React from 'react';
import { TeachingPopoverTitle, TeachingPopoverTitleProps } from '@fluentui/react-teaching-popover-preview';

export const Default = (props: Partial<TeachingPopoverTitleProps>) => <TeachingPopoverTitle {...props} />;
