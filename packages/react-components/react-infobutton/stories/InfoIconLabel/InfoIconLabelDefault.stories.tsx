import * as React from 'react';
import { InfoIconLabel, InfoIconLabelProps } from '@fluentui/react-infobutton';

export const Default = (props: Partial<InfoIconLabelProps>) => <InfoIconLabel {...props} />;
