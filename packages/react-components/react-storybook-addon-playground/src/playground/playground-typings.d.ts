/**
 * Virtual module provided by `webpack.playground.config.js`: URL of the JSON asset that maps virtual
 * `file:///node_modules/...` paths to `.d.ts` contents for the dependency allowlist.
 */
declare module 'playground-typings' {
  const url: string;
  export default url;
}
