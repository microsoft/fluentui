import * as React from 'react';
import { SkeletonCircle, SkeletonCircleProps } from '@fluentui/react-skeleton';

export const Default = (props: Partial<SkeletonCircleProps>) => <SkeletonCircle {...props} />;
