import * as React from 'react';
import { CarouselViewport, CarouselViewportProps } from '@fluentui/react-carousel';

export const Default = (props: Partial<CarouselViewportProps>) => <CarouselViewport {...props} />;
