/**
 * Default timing constants for sequence animations (milliseconds).
 */

import { motionTokens } from '@fluentui/react-motion';

/** Default duration in milliseconds for Hold/Scene components */
export const DEFAULT_HOLD_DURATION = motionTokens.durationNormal;
