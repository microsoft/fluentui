import { Meta } from '@storybook/addon-docs';
import { FullscreenLink } from './utils';
export const parentPath = 'concepts-developer-accessibility-accessibilityexamples';

<Meta title="Concepts/Developer/Accessibility/AccessibilityExamples/List of examples" />

# Accessibility Examples

The goal of accessibility examples is to identify good and bad practice when designing accessible interfaces.

## Labelling examples

- <FullscreenLink content="Avoid action in accessibility name" parent={parentPath} story="avoid-action-example" />
- <FullscreenLink
    content="Avoid component type in accessibility name"
    parent={parentPath}
    story="avoid-component-type-example"
  />
- <FullscreenLink content="Avoid state in accessibility name" parent={parentPath} story="avoid-state-example" />
- <FullscreenLink content="Avoid position in accessibility name" parent={parentPath} story="avoid-position-example" />
- <FullscreenLink
    content="Avoid repeating text for item within one component"
    parent={parentPath}
    story="no-repeat-text"
  />
- <FullscreenLink content="Avoid focus text element" parent={parentPath} story="no-focusable-text" />

## Using aria-label, aria-labelledby and aria-describedby examples

- <FullscreenLink content="Error messages for form fields" parent={parentPath} story="form-error-messages" />
- <FullscreenLink
    content="Label component with visible text"
    parent={parentPath}
    story="label-with-visible-text-example"
  />
