import { Meta } from '@storybook/addon-docs';
import { Spinner } from '@fluentui/react-spinner';

<Meta title="Concepts/Developer/Icons/Icons Catalog" />

### Available Icons

Search through this icon list for all the available icons.

<React.Suspense fallback={<Spinner label="Loading..." />}>
  {React.createElement(React.lazy(() => import('./ReactIconGrid')))}
</React.Suspense>
