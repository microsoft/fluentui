import * as React from 'react';
import { Prototype } from './../utils';

import { AccessibleMeetBase } from './AccessibleMeetBase';

export const UsingGridsActiveOnlyEntireRowNarration: React.FC = () => {
  return (
    <Prototype pageTitle="Variant C: Accessible Meet Using Grids Active Only Entire Row Narration">
      <AccessibleMeetBase variant="gridsActiveOnlyEntireRowNarration" />
    </Prototype>
  );
};
