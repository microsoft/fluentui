import { Meta } from '@storybook/addon-docs';
import { Spinner } from '@fluentui/react-spinner';

<Meta title="Concepts/Developer/Icons/Icons Catalog" />

### Available Icons

This page contains a list of available icons under `@fluentui/react-icons` package. To use icons install the package and select an icon to use.

For example:

```tsx
import { DismissSquareRegular } from `@fluentui/react-icons`;

function App() {
  return <DismissSquareRegular />;
}
```

<React.Suspense fallback={<Spinner label="Loading..." />}>
  {React.createElement(React.lazy(() => import('../../../src/Concepts/Icons/ReactIconGrid')))}
</React.Suspense>
