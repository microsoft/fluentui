{
  "name": "@fluentui/react-tailwind-theme",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Tailwind v4 theme layer for the CSS Modules styling migration: cascade layer order, custom variants, and the base-scale spacing bridge. Fluent design tokens stay runtime CSS custom properties (written by FluentProvider) and are consumed as literal var(--token) — they are intentionally NOT registered here.",
  "style": "css/index.css",
  "exports": {
    ".": "./css/index.css",
    "./css/*": "./css/*"
  },
  "files": [
    "css"
  ],
  "sideEffects": [
    "**/*.css"
  ]
}
