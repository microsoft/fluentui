import * as React from 'react';
import { Button } from '@fluentui/react-modern-components-preview/button';
import type { ButtonProps } from '@fluentui/react-modern-components-preview/button';

export const Default = (props: ButtonProps): React.ReactNode => <Button {...props}>Example</Button>;
