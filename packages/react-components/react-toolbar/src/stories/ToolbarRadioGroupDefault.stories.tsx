import * as React from 'react';
import { ToolbarRadioGroup, ToolbarRadioGroupProps } from '../index';

export const Default = (props: Partial<ToolbarRadioGroupProps>) => <ToolbarRadioGroup {...props} />;
