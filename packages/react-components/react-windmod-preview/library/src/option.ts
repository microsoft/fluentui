export { Option, optionClassNames, useOptionStyles } from './components/Option';
export type { OptionProps, OptionSlots, OptionState } from './components/Option';

/** Headless building blocks, re-exported for consumers composing their own Option. */
export { renderOption, useOption } from '@fluentui/react-headless-components-preview/combobox';
