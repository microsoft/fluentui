/** Positioning primitives, re-exported from the headless package — the same surface
 * `@fluentui/react-components` exposes. windmod adds no styling layer here. */
export type {
  Position,
  Alignment,
  PositioningProps,
  PositioningReturn,
  PositioningImperativeRef,
  PositioningShorthand,
  PositioningShorthandValue,
} from '@fluentui/react-headless-components-preview/positioning';
export {
  usePositioning,
  POSITIONS,
  ALIGNMENTS,
  getPlacementString,
  resolvePositioningShorthand,
} from '@fluentui/react-headless-components-preview/positioning';
