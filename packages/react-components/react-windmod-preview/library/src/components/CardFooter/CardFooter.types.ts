import type {
  CardFooterProps as CardFooterHeadlessProps,
  CardFooterState as CardFooterHeadlessState,
} from '@fluentui/react-headless-components-preview/card';

export type { CardFooterSlots } from '@fluentui/react-headless-components-preview/card';

/** CardFooter has no look props on either layer. */
export type CardFooterProps = CardFooterHeadlessProps;

export type CardFooterState = CardFooterHeadlessState;
