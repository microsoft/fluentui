import type {
  ToolbarGroupProps as ToolbarGroupHeadlessProps,
  ToolbarGroupState as ToolbarGroupHeadlessState,
} from '@fluentui/react-headless-components-preview/toolbar';

/** Windmod ToolbarGroup props. The headless surface carries every prop; windmod adds no look props. */
export type ToolbarGroupProps = ToolbarGroupHeadlessProps;

/** Windmod ToolbarGroup state. */
export type ToolbarGroupState = ToolbarGroupHeadlessState;
