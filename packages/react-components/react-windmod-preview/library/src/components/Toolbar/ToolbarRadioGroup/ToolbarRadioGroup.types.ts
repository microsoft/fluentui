import type {
  ToolbarRadioGroupProps as ToolbarRadioGroupHeadlessProps,
  ToolbarRadioGroupState as ToolbarRadioGroupHeadlessState,
} from '@fluentui/react-headless-components-preview/toolbar';

/** Windmod ToolbarRadioGroup props. The headless surface carries every prop; windmod adds no look props. */
export type ToolbarRadioGroupProps = ToolbarRadioGroupHeadlessProps;

/** Windmod ToolbarRadioGroup state. */
export type ToolbarRadioGroupState = ToolbarRadioGroupHeadlessState;
