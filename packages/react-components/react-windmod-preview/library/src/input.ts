export { Input, inputClassNames, useInputStyles } from './components/Input';
export type { InputAppearance, InputProps, InputSize, InputSlots, InputState } from './components/Input';

/** Headless building blocks, re-exported for consumers composing their own Input. */
export { renderInput, useInput } from '@fluentui/react-headless-components-preview/input';
