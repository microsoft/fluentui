import { Overflow } from '@fluentui/react-components';

export { WithWindmodComponents } from './OverflowWithWindmod.stories';

export default {
  // Documentation-only entry: `Overflow` is renderless and is scoped OUT of the windmod port.
  // The story shows the Griffel `Overflow` driving windmod components unchanged.
  title: 'Windmod/Overflow',
  component: Overflow,
};
