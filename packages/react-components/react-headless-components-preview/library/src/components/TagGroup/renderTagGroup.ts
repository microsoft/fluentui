import { renderTagGroup_unstable } from '@fluentui/react-tags';

/**
 * Renders the final JSX of the TagGroup component, given the state and context values.
 */
export const renderTagGroup = renderTagGroup_unstable;
