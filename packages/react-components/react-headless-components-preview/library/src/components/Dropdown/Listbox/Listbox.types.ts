import type {
  ListboxSlots as ListboxBaseSlots,
  ListboxProps as ListboxBaseProps,
  ListboxState as ListboxBaseState,
  ListboxContextValues as ListboxContextValuesBase,
} from '@fluentui/react-combobox';

export type ListboxSlots = ListboxBaseSlots;

/**
 * Listbox Props
 */
export type ListboxProps = ListboxBaseProps;

/**
 * State used in rendering Listbox
 */
export type ListboxState = ListboxBaseState;

/**
 * Context values used in rendering Listbox
 */
export type ListboxContextValues = ListboxContextValuesBase;
