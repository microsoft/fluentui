import { renderAvatarGroup_unstable } from '@fluentui/react-avatar';

/**
 * Renders the final JSX of the AvatarGroup component, given the state and context values.
 */
export const renderAvatarGroup = renderAvatarGroup_unstable;
