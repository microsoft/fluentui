import { renderMessageBarGroup_unstable } from '@fluentui/react-message-bar';

/**
 * Renders the final JSX of the MessageBarGroup component, given the state.
 */
export const renderMessageBarGroup = renderMessageBarGroup_unstable;
