A CounterBadge displays a numeric count or a dot-only notification state.

It formats counts above `overflowCount`, renders zero only when `showZero` is set, and exposes `data-count`, `data-dot`, `data-hidden`, and `data-overflowed` for styling. Associate the count with its host control's accessible name.
