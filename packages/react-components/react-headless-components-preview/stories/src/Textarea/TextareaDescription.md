A textarea is a multi-line text field for longer free-form input such as messages, descriptions, or notes.

Use Textarea when the expected input may span multiple lines. For single-line structured input like names or email addresses use Input instead. You can control resize behavior via the `resize` prop to allow only vertical, only horizontal, or no resizing at all.
