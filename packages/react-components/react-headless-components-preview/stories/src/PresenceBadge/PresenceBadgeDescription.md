A PresenceBadge communicates a person's availability through an accessible status image.

Supply an `icon` for the visible indicator. The root exposes `data-status` and `data-out-of-office` for styling; the host experience must not rely on color alone to communicate presence.
