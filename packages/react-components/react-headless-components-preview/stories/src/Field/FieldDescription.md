A field is a form-layout wrapper that pairs a label with an input control and optional hint or validation feedback.

Use Field to consistently associate a visible label with any input, select, textarea, or custom control. It manages the ARIA relationship between the label and control, surfaces validation states, and keeps hint text accessible to screen readers.
