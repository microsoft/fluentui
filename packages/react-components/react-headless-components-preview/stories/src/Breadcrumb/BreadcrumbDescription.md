A breadcrumb shows the user's current location within the application's navigational hierarchy.

Use breadcrumbs on pages that are more than two levels deep to help users understand where they are and navigate back to parent sections. The last item typically represents the current page and should be marked with the `current` prop.
