A toggle button represents a two-state button that can be either checked (pressed) or unchecked.

Use toggle buttons for actions that can be switched on or off, such as text formatting controls, mute/unmute, or enabling/disabling a feature. The `data-checked` attribute is set when the button is in the checked state, enabling CSS-based styling without class toggling.
