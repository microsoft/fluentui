`flip-inline` — the browser swaps the inline axis (`before` ↔ `after`) when the requested side would overflow. Same dashed, `contain: layout` box pattern as the block-flip demo.

Native `position-try-fallbacks: flip-inline` swaps the inline axis: `before` ↔ `after`. Compare each popover's **Requested** label with the live **Actual** readout rendered from `[data-placement]`.
