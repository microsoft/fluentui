An input is a single-line text field that accepts keyboard entry from the user.

Use Input for short, structured responses such as names, email addresses, search queries, or codes. For longer free-form text use Textarea instead, and for numeric entry with increment controls use SpinButton.
