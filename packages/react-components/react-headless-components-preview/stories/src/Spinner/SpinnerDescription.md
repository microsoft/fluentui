A spinner communicates that an operation is in progress and the interface is not yet ready for interaction.

Use Spinner for asynchronous operations that take a noticeable amount of time, such as loading data, saving changes, or processing a request. Pair it with a label to describe what is happening. For page-level loading states consider a full-screen overlay; for inline operations place the spinner close to the affected content.
