A select presents a compact list of predefined choices where only one option can be selected at a time.

Use Select when the available values are known in advance and users should choose from a controlled set, such as size, status, language, or category. For free-form text input use Input, and for switching between a few prominent options in view use RadioGroup.
