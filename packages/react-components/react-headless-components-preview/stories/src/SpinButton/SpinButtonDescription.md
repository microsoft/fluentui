A spin button is a numeric input with built-in increment and decrement controls.

Use SpinButton when users need to enter or adjust a number within a defined range, such as a quantity, font size, or timeout value. For unrestricted numeric input use a plain Input with `type="number"`.
