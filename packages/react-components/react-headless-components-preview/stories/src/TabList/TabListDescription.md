A tab list organizes related content into navigable sections, showing one panel at a time.

Use `TabList` when you have two or more distinct views of the same subject — such as Overview, Settings, and Activity — and users need to switch between them. Keep tab labels short and use nouns rather than verbs. Avoid using tabs to replace multi-step workflows; use a stepper or wizard pattern instead.
