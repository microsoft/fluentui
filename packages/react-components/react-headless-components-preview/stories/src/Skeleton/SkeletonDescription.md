A skeleton is a low-fidelity placeholder that previews the shape of content while data is being loaded.

Use Skeleton to reduce perceived load time and prevent layout shift. Place it wherever real content will appear, mirroring the size and shape of the elements it represents. Remove the skeleton once the data has arrived and replace it with actual content.
