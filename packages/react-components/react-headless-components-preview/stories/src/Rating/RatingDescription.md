A rating lets users express satisfaction by selecting a value on a star scale.

Use Rating to collect user feedback on content, products, or experiences. For read-only display of an aggregate score render it in a non-interactive mode by omitting the `onChange` handler. Half-star precision is supported via the `step` prop.
