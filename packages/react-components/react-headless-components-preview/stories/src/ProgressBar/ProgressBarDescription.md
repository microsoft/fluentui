# ProgressBar

A progress bar provides a visual indicator of task completion while content is loading, uploading, or being processed.

Use a determinate ProgressBar when you can calculate completion and provide `value` (and optionally `max`). Use an indeterminate ProgressBar when completion is unknown but work is still active.
