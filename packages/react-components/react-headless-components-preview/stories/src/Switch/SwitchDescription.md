A switch toggles a setting between two mutually exclusive states: on and off.

Use Switch for binary settings that take effect immediately without requiring a submit action, such as enabling notifications or toggling dark mode. For choices that require confirmation before taking effect, use a Checkbox inside a form instead.
