import * as React from 'react';
import { ColorSlider, ColorSliderProps } from '@fluentui/react-color-picker-preview';

export const Default = (props: Partial<ColorSliderProps>) => <ColorSlider {...props} />;
