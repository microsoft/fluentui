import * as React from 'react';
import { ColorArea, ColorAreaProps } from '@fluentui/react-color-picker-preview';

export const Default = (props: Partial<ColorAreaProps>) => <ColorArea {...props} />;
