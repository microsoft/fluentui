import * as React from 'react';
import { AlphaSlider, AlphaSliderProps } from '@fluentui/react-color-picker-preview';

export const Default = (props: Partial<AlphaSliderProps>) => <AlphaSlider {...props} />;
