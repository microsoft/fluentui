import * as React from 'react';
import { NativeSlider, NativeSliderProps } from '@fluentui/react-color-picker-preview';

export const Default = (props: Partial<NativeSliderProps>) => <NativeSlider {...props} />;
