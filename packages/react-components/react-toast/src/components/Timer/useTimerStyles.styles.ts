import { makeStyles } from '@griffel/react';

export const useStyles = makeStyles({
  progress: {
    animationName: {
      from: {
        opacity: 0,
      },
      to: {
        opacity: 0,
      },
    },
  },
});
