import { createContext, createContextSelector } from '@fluentui/global-context';
console.log(createContext, createContextSelector);

export default {
  name: 'createContext and createContextSelector implementations',
};
