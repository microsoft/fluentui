import * as React from 'react';
import type { MotionComponentProps } from '@fluentui/react-components';

/**
 * A helper component for storybook to auto generate an args table for createMotionComponent
 */
export const MotionComponent: React.FC<MotionComponentProps> = props => {
  return <div />;
};
