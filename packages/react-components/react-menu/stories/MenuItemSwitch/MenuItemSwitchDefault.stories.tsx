import * as React from 'react';
import { MenuItemSwitch, MenuItemSwitchProps } from '@fluentui/react-menu';

export const Default = (props: Partial<MenuItemSwitchProps>) => <MenuItemSwitch {...props} />;
