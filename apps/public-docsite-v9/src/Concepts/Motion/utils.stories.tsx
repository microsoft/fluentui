import * as React from 'react';
import type { PresenceComponentProps } from '@fluentui/react-components';

/**
 * A helper component for storybook to auto generate an args table for motion props
 */
export const Motion: React.FC<PresenceComponentProps> = () => {
  return <div />;
};
