import * as React from 'react';
import { CompoundButton } from '@fluentui/react-button';

const Scenario = () => (
  <CompoundButton icon="O" secondaryContent="This is some secondary text">
    Hello, world
  </CompoundButton>
);

export default Scenario;
