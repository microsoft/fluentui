{
  "name": "@fluentui/vr-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Visual regression tests for Fluent UI React",
  "scripts": {
    "build-storybook": "yarn run -T storybook build -o dist/storybook",
    "clean": "yarn run -T just-scripts clean",
    "code-style": "yarn run -T just-scripts code-style",
    "just": "yarn run -T just-scripts",
    "lint": "yarn run -T just-scripts lint",
    "start": "yarn run -T storybook dev -p 3000",
    "type-check": "yarn run -T tsc -p . --noEmit --baseUrl .",
    "test-vr": "node ../../.github/scripts/run-storywright.js STORYWRIGHT_EXCLUDE_PATTERNS_V8 -- --browsers chromium --url dist/storybook --destpath dist/screenshots --waitTimeScreenshot 500 --concurrency 4 --headless true --stepsApi parameters --bailOnStoriesError"
  },
  "dependencies": {
    "@fluentui/example-data": "*",
    "@fluentui/font-icons-mdl2": "*",
    "@fluentui/react": "*",
    "@fluentui/react-charting": "*",
    "@fluentui/react-experiments": "*",
    "@fluentui/react-hooks": "*",
    "@fluentui/react-icons-mdl2": "*",
    "@fluentui/storybook": "*",
    "@fluentui/theme-samples": "*",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "tslib": "^2.1.0"
  }
}
