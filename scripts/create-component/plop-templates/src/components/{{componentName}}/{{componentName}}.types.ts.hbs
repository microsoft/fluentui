import type { ComponentProps, ComponentState, IntrinsicShorthandProps } from '@fluentui/react-utilities';

export type FooSlots = {
  // TODO Add slots here and to {{camelCase componentName}}ShorthandProps in use{{componentName}}.ts
  root: IntrinsicShorthandProps<'div'>;
};

export type FooCommons = {
  // TODO Add things shared between props and state here
};

/**
 * Foo Props
 */
export type FooProps = ComponentProps<FooSlots> & FooCommons;

/**
 * State used in rendering Foo
 */
export type FooState = ComponentState<FooSlots> & FooCommons;
