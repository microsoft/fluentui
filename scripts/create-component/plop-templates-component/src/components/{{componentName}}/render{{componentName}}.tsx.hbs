import * as React from 'react';
import { getSlots } from '@fluentui/react-utils';
import { {{componentName}}State } from './{{componentName}}.types';
import { {{componentName}}ShorthandProps } from './use{{componentName}}';

/**
 * Function that renders the final JSX of the component
 */
export const render{{componentName}} = (state: {{componentName}}State) => {
  const { slots, slotProps } = getSlots(state, {{componentName}}ShorthandProps);

  return (
    <slots.root {...slotProps.root}>
      {state.children}
    </slots.root>
  );
};
