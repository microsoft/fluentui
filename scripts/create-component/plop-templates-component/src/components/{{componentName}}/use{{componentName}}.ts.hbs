import * as React from 'react';
import { makeMergeProps, resolveShorthandProps } from '@fluentui/react-utils';
import { useMergedRefs } from '@fluentui/react-hooks';
import { {{componentName}}Props, {{componentName}}State } from './{{componentName}}.types';

/**
 * Consts listing which props are shorthand props.
 */
export const {{componentName}}ShorthandProps = [];

const mergeProps = makeMergeProps<{{componentName}}State>({ deepMerge: {{componentName}}ShorthandProps });

/**
 * Returns the props and state required to render the component
 */
export const use{{componentName}} = (
  props: {{componentName}}Props,
  ref: React.Ref<HTMLElement>,
  defaultProps?: {{componentName}}Props,
): {{componentName}}State => {
  const state = mergeProps(
    {
      ref: useMergedRefs(ref, React.useRef(null))
    },
    defaultProps,
    resolveShorthandProps(props, {{componentName}}ShorthandProps),
  );

  return state;
};
