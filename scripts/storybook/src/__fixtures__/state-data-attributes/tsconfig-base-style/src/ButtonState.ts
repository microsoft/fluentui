/**
 * State for Button component extracted from a tsconfig.base-style project
 * that deliberately omits the `jsx` compiler option.
 */
export type ButtonState = {
  root: {
    /** Whether the button is disabled. */
    'data-disabled'?: 'true' | 'false';
  };
};
